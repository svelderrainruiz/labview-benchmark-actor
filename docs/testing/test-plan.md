# labview-benchmark-actor — Test Plan

> Standards baseline: `repo-standards-review` v0.2.19. Test planning follows
> ISO/IEC/IEEE 29119-2 (test processes) and 29119-3 (test documentation). This
> is a planned approach; no test results are claimed until implementation.

## Test approach

- **Deterministic first.** Pure logic (cursor→time mapping, time→picture
  indexing, run-result schema, bus message framing/ordering) is covered by
  fast, host-independent unit tests.
- **Transport integration.** The TCP/UDP bus is exercised with in-process /
  loopback peers before any multi-VM run.
- **Host-native / deployment.** Install and multi-VM behavior is validated on
  the real Codespace and Vagrant targets (maintainer-run; not a hosted CI gate).
- **Traceability.** Every `LBA-REQ` maps to at least one test item below
  (29119-3 test-case-to-requirement traceability).

## Test items

| ID | Requirement | Level | Approach |
| --- | --- | --- | --- |
| T-001 | LBA-REQ-001 | Build/package | Package the `.vsix`; assert no `vi-history-suite`-private module on the dependency graph; verify the moved-module manifest matches the packaged surface. |
| T-002 | LBA-REQ-002 | Deployment | Install the same artifact on a Codespace and a Vagrant golden VM; assert activation first-run signal on both; assert prerequisite checks fire with remediation when a prerequisite is absent. |
| T-003 | LBA-REQ-003 | Unit + integration | Validate a run result against its schema; assert metrics and pictures share one run clock; assert reproducibility within the documented variance bound. |
| T-004 | LBA-REQ-004 | Unit (viewer logic) + browser | Pointer and keyboard drag map to a selected time within bounds; Home/End jump to run start/end; selected time updates continuously; no out-of-range selection. |
| T-005 | LBA-REQ-005 | Unit (indexing) + browser | Nearest-at-or-before rule resolves the correct picture index; moving the cursor updates the picture in lockstep; the "no frame at this time" state renders when appropriate. |
| T-006 | LBA-REQ-006 | Deployment | Spawn N VMs from the declarative topology; assert each activates with a unique identity and publishes results; assert clean teardown leaves no orphaned listeners/locks. |
| T-007 | LBA-REQ-007 | Integration | TCP delivers ordered claim/handoff/ack/done; UDP presence beacons flow; a dropped UDP beacon does not corrupt TCP-ordered state; a dropped TCP peer is detected; a late joiner reconstructs session state; no path touches `github.com` at run time; assert the bus carries **inter-actor communication only** — no run data, run/frame metadata, or images. |
| T-008 | LBA-REQ-008 | Static / CM | Assert `README.md` and `docs/cm/cm-plan.md` name `repo-standards-review` v0.2.19 (commit `d44f210d`); assert the `docs/` lane layout matches the standards runner; assert requirement IDs are unchanged after a simulated move. |
| T-009 | LBA-REQ-009 | Integration | Assert captured pictures are written to the VM-local mprr **long-packet** ring buffer and their index/timestamp to the **short-packet** stream (per mprr ADR-0024); assert the run-result frame `ref` resolves against the local mprr review-capture store; assert **nothing from the ring buffer crosses the bus** (the bus is inter-actor comms only). |
| T-010 | LBA-REQ-010 | Integration + static | Assert the viewer operates over the actor's own local run history (no cross-VM read, no run data on the bus); assert completed runs are concentrated to the operator's host by an explicit out-of-band step (not the bus); assert the host-side ollama comparison layer consumes the concentrated corpus. |
| T-011 | LBA-REQ-011 | Unit (deterministic logic) | Build the correlation over a synthetic CPU/RAM/disk series with a pre/post trigger; assert each sample maps to the correct frame index (null before frame zero), the trigger resolves to a frame index, samples split pre/post correctly, and each metric's pre/post window (count/mean/min/max) and post-minus-pre delta are correct; assert null counters are skipped and invalid input fails closed. |
| T-012 | LBA-REQ-012 | Deterministic (embed round-trip) | Assert `lbabus agents` emits the version-pinned base instructions embedded in the binary and that `agents --out`/`--check` round-trip (embedded == source, exit 0); assert a tampered copy is detected as drift (exit 3). Gated on every release by the `ci-agents` harness stage; the reqs-coverage check (`experiments/reqs-coverage/verify-reqs-coverage.mjs`) enforces that this row's cited evidence resolves. |
| T-013 | LBA-REQ-013 | Retired (superseded) | RETIRED under ADR-0048: the priority + addressing envelope (`post --priority`, `poll`/`wait --to-me`/`--min-priority`, the `CollabMessage`/`Priority` model + the fixture-priority mock cases) was removed with the GitHub-Discussion transport (ADR-0047). The live-only `net` bus carries no priority/addressing, so there is nothing to assert; LBA-REQ-013 is superseded, not tested. | `prio` tier (P0>P1>P2>P3, default P2) and the sender's `agentId`; `poll`/`wait --to-me` keeps only broadcast-or-self-addressed messages and drops other-plane traffic; `poll`/`wait --min-priority` keeps only messages at least that urgent. Assert back-read-compat: a flat additive envelope parses through the real `CollabMessage.TryParse` while a nested-object envelope and a `schema@v2` bump each silently drop. Gated by the `fixture-priority` mock cases (`linux-to-me-recipient-filter`, `linux-min-priority-triage-filter`, `linux-envelope-flat-additive-back-read-compat`) in the ci harness. |
| T-014 | LBA-REQ-014 | Deterministic (fixture) + maintainer (cross-plane) | Assert the mprr short-ring core is deterministic and has teeth (`verify-mprr-ring` 9/9: ring write/wrap/zero-copy-view, overwrite fail-closed, blockId + boundary-variation, admission fail-closed, deterministic authoritative ingest, jittered non-authoritative, non-monotonic rejection, viewer-series projection determinism). Assert the store pairs two planes' runs of a shared `benchmarkId` and reports numeric `deltas` + a `digests` section where the deterministic `seriesHash` matches (`match:true`) and the per-plane screenshot `pngSha256` is a witness (`match:false`), failing closed on a single-plane compare (`verify-benchmark-store` 6/6). Gated by local gates #27 (`benchmark-store-receipt-green`, asserts the seriesHash matches cross-plane) and #28 (`mprr-short-ring-model-green`). Maintainer cross-plane evidence: each plane runs `node playwright/screenshot.mjs` + `node experiments/benchmark-store/register-mprr-run.mjs`; PROVEN 2026-07-31 — a real WIN Node run (win32/x64, Node v22.15.0) independently produced the identical deterministic `seriesHash` and `cross-plane-comparison-proven-green` gates the cross-plane match + zero metric deltas. The synthetic identical-to-LINUX WIN `pngSha256` placeholder was removed; the per-plane WIN screenshot visual witness is a pending maintainer step (browser, non-CI). |
| T-015 | LBA-REQ-015 | Deterministic (fixture) + maintainer (real report) | Assert `summarizeViAnalyzerReport` is deterministic + ORDER-INDEPENDENT (`verify-vi-analyzer-result` 6/6: counts + verdict, order-independent resultHash, all-pass, resultHash-tracks-content, benchmark-metrics projection, rejects invalid result + duplicate viPath). Gated by local gate #30 (`vi-analyzer-result-model-green`). The Windows clean room installs the toolkit (`cleanroom/docker-windows/install-vi-analyzer.ps1`, PR #72). Maintainer real-report evidence: a plane runs `LabVIEWCLI RunVIAnalyzer`, the report is summarized, and the `resultHash` compared cross-plane; Proven when a real report is summarized on both planes and the `resultHash` matches. |
| T-016 | LBA-REQ-016 | Static / CM | Assert `docs/cm/cm-plan.md` states the GitFlow feature/release/hotfix branch rules (feature from and back into `develop`; release from `develop`, merged to `main` and `develop`, then deleted; hotfix from `main`, merged to `main` and `develop`), the merge method by branch type (squash for feature; `--no-ff` merge commit for release/hotfix back-merges), SemVer tags on `main`, and coverage retained on the tagged release path; assert `docs/architecture/adr/ADR-0010-gitflow-branch-governance.md` records the decision. Mirrors the authoritative `repo-standards-review` CM gate (all 9 GitFlow signals + SemVer + coverage-on-release), which reports `cm` PASS. Gated by `gitflow-branch-governance-documented` in `verify-local-gates`. |
| T-017 | LBA-REQ-017 | Deterministic (offline) | Assert the committed `dep-manifest.json` passes the verifier, and that the verifier FAILS CLOSED on each defect class (bad schema, malformed SHA, unknown plane, missing python bitness, bad `pinStatus`, resolved-but-empty version), while allowing `tbd-*` pins to omit their concrete value. Gated in `verify-local-gates`. |
| T-018 | LBA-REQ-018 | Deterministic (offline, mock adapter) | Assert the delegation harness end-to-end via the mock provider (no GPU / no network): `lba-uplift-task@v1` validation accepts a good task and rejects bad schema/domain; the provider seam returns the `{provider,model,text,ms,ok}` contract; the acceptance gate passes a good draft and FAILS a weak one; the receipt matches `lba-uplift-delegation-receipt@v1`; the registry routes a `CLAIM` only to a live capability-matched worker; the worker pool bounds concurrency; and each uplift domain (coverage-lift, evidence, risky-test, vipm-gate, vipm-routing) gates fail-closed. Gated in `verify-local-gates`. |
| T-019 | LBA-REQ-019 | Deterministic (offline, host-free) | Assert the MCP surface across three legs (no real VS Code, no display, no live `lbabus`): the pure JSON-RPC handler answers `initialize` / `tools/list` / `tools/call` and returns `-32601` / `-32602` for an unknown method / tool; `activate()` registers the MCP provider under the SAME id the manifest contributes; and the spawned stdio entry round-trips `initialize` + `tools/list` + `tools/call` (deterministic `get_benchmark_series`) over newline-delimited JSON-RPC. The bundled `scripts/mcpToolDoc.mjs --check` keeps `docs/mcp-tools.md` in sync. Gated in `npm test`. |
| T-020 | LBA-REQ-020 | Deterministic (offline) | Assert the release gate clears a `<component, version>` only when BOTH required planes record `agreed:true`, and FAILS CLOSED on a missing / withheld / unparseable sign-off (exit 1) or a usage error (exit 2); covers the bare-SemVer + tagged-form (`collab-cli-vX.Y.Z` / `ext-vX.Y.Z`) normalization and the `--component` selector. Wired before the publish job in both `extension-release.yml` and `collab-cli-release.yml`. |
| T-021 | LBA-REQ-021 | Deterministic (offline) | Assert the correspondence engine FAILS CLOSED (exit 1) when a governed test file (`test/*.mjs`, `experiments/**/verify-*.mjs`, `*.selftest.mjs`, `*.playwright.{mjs,cjs}`, `playwright/*.mjs`, `tools/**/verify-*`) is absent from every RTM CodeRef (rule TR-1), and PASSES when all correspond; AD-1 (every ADR traces to a requirement and is registered in overview.md) and VW-1 (every requirement described by an architecture view) also FAIL CLOSED on a broken correspondence. Gated in `verify-local-gates`. |
| T-022 | LBA-REQ-022 | Deterministic (offline) | Assert the generator DERIVES the traceability matrix from the canonical sources (SRS ids/titles, RTM status/TestID/CodeRef, `overview.md` addressing view, ADR index "Traces to") and that `--check` FAILS CLOSED (exit 1) when `docs/requirements/traceability-matrix.md` drifts from the sources, and PASSES when current. Gated by `traceability-matrix-current` in `verify-local-gates`. |
| T-024 | LBA-REQ-024 | Deterministic (offline) | Assert the corroboration quorum compare (`compare-witnesses.mjs`) computes the tiered-anchor graded confidence and PASSES only on a >=majority concurrence over the OS-independent anchors with a distinct-environment (non-N-of-a-kind) witness set and a consensus gate verdict of `pass`; FAILS CLOSED on a sub-majority OS-independent divergence, an N-of-a-kind set, or a non-`pass` verdict; tolerates one Linux-only (`pngSha256`) divergence by majority; a single-witness input yields an error. Self-test 7/7, gated by `acg-quorum-compare-witnesses` in `verify-local-gates`. Also assert the witness-bundle assembler (`assemble-witness.mjs`) maps a witness's gate/render/capability receipts into the canonical quorum bundle, FAILS CLOSED on any missing release-gating anchor (verdict/version/sourceCommit/seriesHash/pngSha256, or a Linux witness's Ubuntu codename), and that three assembled witnesses corroborate end to end through the quorum (self-test 9/9, gated by `acg-quorum-assemble-witness`). Also assert the committed real-grid evidence corroborates -- a noble codespace bundle + the host LINUX bundle (both lbabus 0.13.0 @ the same commit, byte-identical seriesHash+pngSha256) yield verdict=pass, re-derived tamper-evidently by `acg-quorum-live-corroboration`, tolerating only the graded Ubuntu-codename divergence. |
| T-025 | LBA-REQ-025 | Deterministic (offline) | Assert the provenance/attestation engine (`attest.mjs`) signs a witness bundle into an Ed25519 enrolled-key attestation and VERIFIES it, FAILING CLOSED on a tampered bundle, an un-enrolled identity, a rogue key presented for an enrolled identity, or a corrupted signature; and that verify-before-consume BLOCKS a release unless every witness attestation verifies against the enrolled allowlist, the witnesses are distinct enrolled identities (non-N-of-a-kind), and the quorum re-computed over exactly the attested bundles passes. Self-test 10/10, gated by `acg-provenance-attest` in `verify-local-gates`. Also assert the live enrolled-key chain: the committed codespace + host attestations over their real bundles verify against the committed enrollment allowlist, yielding consume:true (distinct identities, re-computed quorum pass), re-derived tamper-evidently by `acg-provenance-verify-before-consume`. |
| T-026 | LBA-REQ-026 | Deterministic (offline) | Assert the witness-independence engine (`independence.mjs`) counts a witness toward the quorum ONLY when its plane/os environment is enrolled, its identity is recorded in the provenance, and it does not duplicate an already-counted environment; a quorum is independent iff >= quorumMin distinct enrolled environments remain, else it is rejected (fail-closed). Covers distinct->independent, N-of-a-kind rejected, non-enrolled excluded, duplicate collapses, identity-less excluded, cross-OS independent, single-witness rejected, and `assertIndependentQuorum` throwing. Self-test 9/9 gated by `acg-independence-quorum`; the committed live {CODESPACE/linux, LINUX/linux} grid is independent with recorded identities, re-derived tamper-evidently by `acg-independence-live`. |
| T-030 | LBA-REQ-030 | Deterministic (offline) | Assert the base-branch policy (`pr-base-branch-guard.mjs` `evaluateBasePolicy`) allows non-release heads onto develop (or any non-main base) and permits ONLY release/* and hotfix/* onto main -- BLOCKING develop->main (main never takes develop directly) and feature/authoring->main -- with refs/heads/ normalization and a missing base blocked. Self-test 11/11 gated by `acg-governance-pr-base-branch`; the `.github/workflows/pr-base-branch-guard.yml` workflow (runs on PRs targeting main, invokes the guard) is drift-checked by `acg-governance-pr-base-branch-workflow-wired`. |
| T-031 | LBA-REQ-031 | Deterministic (offline) + live evidence | Assert the Merkle transparency log (`transparency-log.mjs`) uses RFC 6962 domain-separated hashing (leaf `0x00`, node `0x01`, largest-power-of-2 split -- verified against hand-computed n=1/2/3 known answers), that every leaf at sizes 1..9 has an inclusion proof reconstructing the root and every prefix has a consistency proof the new head honors, and that Ed25519 signed tree heads verify -- all FAILING CLOSED on a substituted leaf, wrong index, tampered path/root, rewritten history, or rogue key. Self-test 26/26 gated by `acg-transparency-log`. Live evidence: the real {codespace, host} attestations recorded under one signed head, both proven included, re-derived by `acg-transparency-log-live`; and the reviewer-workstation verifier (`verify-release-inclusion.mjs`) ADMITS the real `release-provenance-bundle.json` (>=2 witnesses attested + logged) yet BLOCKS a tampered one (`acg-transparency-verify-before-install`), wired fail-closed into `reviewer-workstation/provision.ps1` before the .vsix install (`acg-transparency-verify-before-install-wired`). |
| T-032 | LBA-REQ-032 | Unit (deterministic logic) + real data | Assert the mesh-stress-signature engines: the signature extractor derives per-counter features (mean/std/percentiles/drift slope/autocorrelation periodicity) plus across-repeat stability (a feature is signature when its across-repeat coefficient-of-variation is within threshold, else noise) plus MAD outliers (with a std fallback when over half the samples are identical) plus cross-counter outlier co-occurrence within +/-200 ms, over synthetic cases and the real exact-12-FPS Linux capture split into repeats (self-test 5/5, gate `mesh-stress-signature-extractor`); the calibration-curve fitter maps a synthetic idle..saturate ladder to a per-rung expected value + tolerance band, scores the monotone/separable/repeatable invariants, drops non-tracking features, and inverse-reads an observed signature to its rung with a confidence (self-test 4/4, gate `mesh-stress-signature-calibrator`); the stress orchestrator emits the monotone commanded ladder with a per-actor VirtualBox throttle + host/guest stress-ng commands + a ladder plan pinning each actor to a distinct level (self-test 5/5, gate `mesh-stress-orchestrator`). |
| T-033 | LBA-REQ-033 | Deterministic (offline replay) + hybrid live provisioning | Assert the personal golden-VM onboarding First Win is COVERED by composition (ADR-0023 Phase 1): `validateFirstWinReceipt` confirms the committed `first-win-onboarding@1` receipt composes all six roadmap flow steps to real, committed, Proven-slice realizations on disk (`lba init` -> build-virtualbox.sh; provision-guest.sh [LBA-REQ-044]; provisionerReadiness.mjs [LBA-REQ-049]; hybrid activation; probe-activation.sh [LBA-REQ-038]; registerMeshActor.mjs [LBA-REQ-039]) and that activation was confirmed live on `lba-golden` (headless RunVI 20+22=42); the checker FAILS CLOSED on a missing realization, an unconfirmed activation, a forged completeness verdict, or a tampered digest. Self-test 7/7 gated by `first-win-onboarding` in `verify-local-gates`. |
| T-034 | LBA-REQ-034 | Deterministic (offline) | Assert the ISO/IEC/IEEE 26514:2022 information-for-users conformance checker: the committed bounded product set is conformant (10 required items present + non-trivial, the command reference covers every contributed VS Code command, the conformance boundary states a bounded product claim and disclaims full process conformance, and the navigation hub indexes the set), and the checker FAILS CLOSED on an empty set (a finding for every missing item plus any uncovered command). Self-test 2/2 gated by `information-for-users-26514` in `verify-local-gates`. |
| T-035 | LBA-REQ-035 | Deterministic (offline) | Assert the ISO/IEC/IEEE 29119-3 test report + ISO 10007 status-accounting generator: the committed `docs/testing/test-report.md` is CURRENT with the canonical sources (the fail-closed gate inventory, the 7 correspondence rules, the coverage floors, and the requirement/ADR/test-item status accounting all derive from the apparatus), rendering is DETERMINISTIC (two renders byte-identical, so `--check` is a reliable drift gate), and the drift compare FAILS CLOSED on any mutation of the report. Self-test 4/4 gated by `test-report-current` in `verify-local-gates`. |
| T-036 | LBA-REQ-036 | Deterministic (offline) | Assert the ISO/IEC/IEEE 15289 release-procedure checker: the committed `docs/release/release-procedure.md` cites only resolvable enforcement points (every `.github/workflows/*`, `experiments/**`, `reviewer-workstation/*`, and action path resolves on disk) and names every required release invariant (SemVer tag on main, bidirectional agreement, keyless signing, rekor, transparency-log inclusion, verify-release-inclusion, `--no-ff`), and the checker FAILS CLOSED on a procedure that cites a missing file or drops a required invariant. Self-test 3/3 gated by `release-procedure-references-resolve` in `verify-local-gates`. |
| T-037 | LBA-REQ-037 | Deterministic (offline) | Assert the continuous-compliance self-audit: the committed repository scores 25/25 across the five-lens rubric (REQ/ARCH/TEST/CM/DOC) over 41 clause-evidence checks (real information items + wired gates + clause anchors), the generated `docs/compliance/compliance-posture.md` scorecard is current, and the scoring FAILS CLOSED when any single clause-evidence item is missing (an all-false probe scores every lens 0; dropping one file pulls its lens below target). The deep-compliance artifacts (test report, release procedure) are load-bearing across lenses. Self-test 4/4 gated by `continuous-compliance-self-audit` in `verify-local-gates`. |
| T-038 | LBA-REQ-038 | Deterministic (offline replay) + live probe evidence | Assert the LabVIEW activation-confirmation contract (ADR-0023 Phase 1): the committed REAL capture (headless `LabVIEWCLI RunVI` on the shipped known-answer `AddTwoNumbers.vi`, LabVIEW 2026) deterministically rebuilds `activation-receipt@1` (digest over verdict-bearing fields only, so it replays offline in CI), the receipt validates as ACTIVATED (20 + 22 = 42, exit 0), and the confirmation FAILS CLOSED on a non-zero exit, a wrong known-answer value, a missing success line, a tampered digest, or a verdict contradicting the result rule. Self-test 5/5 gated by `activation-receipt-confirms-activation` in `verify-local-gates`. |
| T-039 | LBA-REQ-039 | Deterministic (offline) | Assert mesh-actor registration is gated on activation (ADR-0023 invariant): `registerGoldenActor` validates the committed `activation-receipt@1` and only then composes the golden `mesh-actors.csv` row (`golden,golden,actor,actor,192.168.56.10,7420,7421,both,AGENT_GENERATED`), registration is idempotent (re-registering replaces the golden row and preserves existing mesh rows), and an unactivated (exit!=0 / verdict false) or tampered-digest receipt is REFUSED with the registry left untouched. Self-test 4/4 gated by `mesh-actor-registration-requires-activation` in `verify-local-gates`. |
| T-040 | LBA-REQ-040 | Deterministic (offline replay) + live 3-instance evidence | Assert the distributed capacity-weighted parallel workload (ADR-0028): the committed receipt of a real run (host + 2 codespaces, 42 self-tests) validates — the capacity-weighted split re-derived from the recorded per-instance weights reproduces the disjoint shards, the shards cover every task, the instances are distinct (3 hostnames), every instance attests ripgrep-only, and every task passed (25/9/8) — and validation FAILS CLOSED on overlapping shards, a shared instance, a non-ripgrep tool, or any task failure. Self-test 4/4 gated by `distributed-parallel-workload` in `verify-local-gates`. |
| T-041 | LBA-REQ-041 | Deterministic (offline replay) + live 3-instance evidence | Assert capability-aware distributed routing (ADR-0029): the committed receipt of a real run (a `LabVIEWCLI RunVI` activation probe + 43 node self-tests across host + 2 codespaces) validates — the LabVIEW task ran on the LabVIEW-capable host, the routing re-derived from the recorded capabilities + weights reproduces the shards, every task ran capability-matched, the shards are disjoint + cover all + distinct-instance + ripgrep-only, and every task passed — and validation FAILS CLOSED on a LabVIEW task placed on a node-only instance, a non-ripgrep instance, or any failure; `routeByCapability` throws on an unsatisfiable capability. Self-test 5/5 gated by `capability-aware-routing` in `verify-local-gates`. |
| T-042 | LBA-REQ-042 | Deterministic (offline replay) + live 2-plane evidence | Assert cross-plane LabVIEW liveness (ADR-0030): the committed receipt of a real run (this host + the Ubuntu golden VM `lba-ubuntu2404-labview2026-scratch`, both LabVIEW 2026, each running `LabVIEWCLI RunVI` on AddTwoNumbers concurrently) validates — >= 2 distinct planes, each returned the known answer (7 + 5 = 12), reported RunVI success, and is activated, all live — and validation FAILS CLOSED on a wrong answer, a single plane, two planes on the same host, or allActivated=false. Self-test 4/4 gated by `cross-plane-labview-liveness` in `verify-local-gates`. |
| T-043 | LBA-REQ-043 | Deterministic (offline replay) + live 2-plane evidence | Assert cross-plane VI Analyzer determinism (ADR-0031, North Star): the committed receipt of a real run (this host + the Ubuntu golden VM, each running the same LabVIEWCLIExampleProject VI Analyzer config, 69 tests) validates — >= 2 distinct planes each carry a resultHash and ALL resultHashes are identical (the consensus) — and validation FAILS CLOSED on a divergent plane hash, a single plane, or a forged resultHashesMatch. Self-test 4/4 gated by `cross-plane-vi-analyzer-determinism` in `verify-local-gates`. |
| T-044 | LBA-REQ-044 | Deterministic (offline) + live install evidence | Assert the from-scratch provisioner installs LabVIEW + VIPM (ADR-0023 Phase 1): `checkProvisioner` confirms `cleanroom/ubuntu-labview/provision-guest.sh` installs `ni-labview-2026-community` (NI apt repo + committed keyring) AND VIPM from the JKI package server (`dpkg -i` + `apt-get install -f`, idempotent via `dpkg -s vipm`), the committed NI keyring exists, and the live receipt confirms VIPM 26.3.1-4000 was installed on the scratch VM; the checker FAILS CLOSED when the LabVIEW or VIPM step is missing or the receipt does not confirm VIPM. Self-test 4/4 gated by `provisioner-installs-labview-and-vipm` in `verify-local-gates`. |
| T-045 | LBA-REQ-045 | Deterministic (offline) + live bridge evidence | Assert the human-assisted VM bridge (ADR-0032): `checkVmBridge` confirms `tools/vm-bridge/vm-bridge.sh` drives the VM shell via tmux `send-keys`/`capture-pane` over ssh, offers a human `attach` and a `secret?` prompt detector, and is SECRET-SAFE (no `--password`/`--token` flag, no `read -s`, no `sshpass`, no credential env var), and the live receipt shows the agent drove the scratch VM and a real `password:` prompt was detected (agent exit 42) + handed off, never answered; the checker FAILS CLOSED on a secret-ingesting bridge or a receipt where the agent answered a prompt. Self-test 4/4 gated by `vm-bridge-human-assisted-secret-safety` in `verify-local-gates`. |
| T-046 | LBA-REQ-046 | Deterministic (offline) + live install evidence | Assert VIPM functionally installs a LabVIEW community package on the golden VM (ADR-0023 Phase 1): `validateVipmInstallReceipt` confirms the committed receipt records the operator-designated self-test package g-cli (`wiresmith_technology_lib_g_cli` 3.0.1.98) plus its dependency closure (LUnit, LUnit-for-G-CLI, Rainbow Terminal), each installed cleanly (`No Errors`, > 0 files-installed in the VIPM package DB) with 279 files landing under `vi.lib`, and it replays deterministically (rebuild -> identical digest); the checker FAILS CLOSED when any package did not install cleanly, no files landed in vi.lib, the designated package is absent, the verdict is forged, or the digest is tampered. Self-test 8/8 gated by `vipm-functional-package-install` in `verify-local-gates`. |
| T-047 | LBA-REQ-047 | Deterministic (offline) + live capture evidence | Assert live golden-VM visibility + idle-time analysis (ADR-0023 Phase 1): `validateStatusTimelineReceipt` confirms the committed REAL timeline receipt (a 44s capture from `lba-golden` over the bridge, with a mid-capture CPU burst) re-derives its idle-time analysis exactly from the samples -- contiguous idle vs busy spans, idle % (63.6%), longest idle run (18s) -- and the digest is intact; the analyzer discriminates idle from busy (all-idle -> 100%, all-busy -> 0%); the checker FAILS CLOSED on a stale/tampered analysis, a tampered digest, a < 2-sample series, or non-monotonic sample times. Self-test 7/7 gated by `vm-live-status-idle-analysis` in `verify-local-gates`. |
| T-048 | LBA-REQ-048 | Deterministic (offline) + live benchmark evidence | Assert the golden-VM Mass Compile benchmark (ADR-0023 Phase 1; replaces the deferred VI Analyzer benchmark): `validateMassCompileReceipt` confirms the committed REAL receipt (LabVIEWCLI MassCompile of the `ni/labview-icon-editor` `resource/` source on `lba-golden` = 307 VIs/CTLs, 0 bad, "operation succeeded", ~24s) re-derives its machine-independent `resultHash` (directory + VI count + bad count + success, timing-invariant so it is cross-plane comparable) and verdict, and the digest is intact; the checker FAILS CLOSED on a tampered resultHash, a forged verdict, a bad-VI list inconsistent with its count, or a tampered digest. Self-test 7/7 gated by `mass-compile-benchmark` in `verify-local-gates`. |
| T-049 | LBA-REQ-049 | Deterministic (offline) static verification | Assert the golden-VM provisioner is headless-LabVIEW ready (ADR-0023 Phase 1, the one-command First Win): `validateReadinessReceipt` re-derives the required checks from the ACTUAL `cleanroom/ubuntu-labview/provision-guest.sh` text and confirms the committed receipt records all of them present and ready -- Xvfb install, VI Server (:3363) config in BOTH `labview.conf` and `labviewcommunity.conf`, quoted access lists, and the post-install reboot; the checker FAILS CLOSED if the provisioner drops any step, a ready verdict is forged, or the digest is tampered. Self-test 7/7 gated by `provisioner-headless-readiness` in `verify-local-gates`. |
| T-050 | LBA-REQ-050 | Deterministic (offline) + live cross-plane evidence | Assert the cross-plane benchmark grid (ADR-0031; roadmap Phase 4): `validateBenchmarkGrid` confirms the grid derived from the committed per-benchmark receipts records each benchmark's identity agreement across planes and is OK -- VI Analyzer (host + scratch VM, resultHash 0419a449) and Mass Compile of the icon-editor `resource/` source (host + lba-golden, resultHash bf722123) each agree on identity across their planes -- Mass Compile across the OS axis (host + lba-golden Linux + win-VITLT-SERGIO Windows LabVIEW 2026, 3/3), with the 39s/24s/211s compile time as the performance metric; the committed `docs/benchmarks/benchmark-grid.md` is current; the checker FAILS CLOSED on a determinism violation (planes disagreeing on identity), a forged agreement/verdict, or a tampered digest. Self-test 7/7 gated by `cross-plane-benchmark-grid` in `verify-local-gates`. |
| T-051 | LBA-REQ-051 | Deterministic (offline) + live build evidence | Assert the icon-editor Packed Library build benchmark -- the builder actor of the 2-actor icon-editor grid (ADR-0033): `validatePplReceipt` confirms the committed REAL receipt (LabVIEWCLI ExecuteBuildSpec of the "Editor Packed Library" spec of `lv_icon_editor.lvproj` in the NI LabVIEW container `nationalinstruments/labview:2026q1-linux` -> `lv_icon.lvlibp`, 2.9 MB, 59s) re-derives its machine-independent `resultHash` (project + target + build spec + generated artifact + success) and verdict, and the digest is intact; the checker FAILS CLOSED on a tampered resultHash, a forged verdict, a build that produced no packed library, or a tampered digest. Self-test 7/7 gated by `ppl-build-benchmark` in `verify-local-gates`. |
| T-052 | LBA-REQ-052 | Deterministic (offline) + live build/round-trip evidence | Assert the g-cli launcher proof -- the tester-actor enabler of the 2-actor icon-editor grid (ADR-0033): `validateGcliReceipt` confirms the committed REAL receipt (the `rust-proxy` crate `G-CLI/G-CLI` `@662f75a` built with `cargo build --release` -> `g-cli` 3.0.1, then a full host round-trip: g-cli launched `Echo Parameters.vi` on LabVIEW 2026, which echoed `hello/from/host` over TCP and exited 0) re-derives its machine-independent `resultHash` (tool + version + source commit + operation + args in + echoed text + exit code + LabVIEW version/bitness) and verdict, the echo matches the args sent, and the digest is intact; the checker FAILS CLOSED on a tampered resultHash, a forged verdict over a failed round-trip, an echo that does not match the args sent, or a tampered digest. Self-test 7/7 gated by `g-cli-proxy-proof` in `verify-local-gates`. |
| T-053 | LBA-REQ-053 | Deterministic (offline) + live test-run evidence | Assert the icon-editor LUnit test benchmark -- the tester actor of the 2-actor icon-editor grid (ADR-0033): `validateLunitReceipt` confirms the committed REAL receipt (the Rust-built g-cli ran `g-cli lunit -- -r <report> lv_icon_editor.lvproj` on `lba-golden`, LUnit from `icon-editor-developer.vipc`, 4 classes / 25 cases: 10 passed, 2 failed, 8 errored headless, 5 setup) re-derives its machine-independent `resultHash` (the sorted `class/case` inventory + suite structure + total) and verdict, the inventory length matches the total, and the digest is intact; the checker FAILS CLOSED on a tampered resultHash, a forged verdict over an empty run, an inventory that disagrees with the total, or a tampered digest. Self-test 7/7 gated by `lunit-test-benchmark` in `verify-local-gates`. |
| T-054 | LBA-REQ-054 | Deterministic (offline, derived) | Assert the Benchmark Observatory -- the suite-wide map above the cross-plane grid (ADR-0034): `validateObservatory` confirms the observatory DERIVED from every committed benchmark receipt (VI Analyzer + Mass Compile host/golden/win + icon-editor PPL build + LUnit test) re-derives its coverage matrix (benchmark-type x plane), determinism ledger (identity agreement per benchmark), and verdict, and that the committed `docs/benchmarks/benchmark-observatory.md` is current; the checker FAILS CLOSED on a determinism violation (planes disagreeing on identity), a coverage matrix that contradicts the receipts, a forged verdict, or a tampered digest. Self-test 8/8 gated by `benchmark-observatory` in `verify-local-gates`. |
| T-055 | LBA-REQ-055 | Deterministic (offline) + live evidence | Assert the Handoff Beacon capture-status payload (ADR-0035): `buildCaptureStatus` derives from a capture's resource samples the `wroteToDisk` flag (a per-disk write rate above the threshold for the minimum sample count), the peak write MB/s + disk + the frame index where it peaked, and a per-physical-disk write/read peak breakdown; `buildCapturingStatus`/`buildFailedStatus` cover the other lifecycle states; and `validateCaptureStatus` FAILS CLOSED on a wrong schema, an unknown state, or a stopped/failed beacon missing its payload. Self-test 6/6 gated by `handoff-capture-status` in `verify-local-gates` (+ an inline integration check that a real launch-capture with per-disk throughput yields a coherent stop beacon pointing at the peak-write frame). The correlator auto-jump helpers (`peakFrameIndexOf`, `readPeakFrameIndex`, `clampFrameIndex`, `buildCorrelatorModel`) are unit-covered in the extension test suite (the correlator opens on the peak-write frame, clamped into range) and asserted by `panels-render` (a non-zero `selectedIndex` renders that frame's readout). Live evidence: the reviewer VM's streaming VI produced a `stopped` beacon (wroteToDisk=true, peak 134 MB/s @ frame 1122) that `await-handoff.sh` resolved. |
| T-056 | LBA-REQ-056 | Deterministic (offline) + live evidence | Assert the Handoff Beacon agent->human request payloads (ADR-0036): `buildAgentRequest`/`buildOpDone` derive the `agent-request@1`/`op-done@1` beacons; `validateAgentRequest`/`validateOpDone` FAIL CLOSED on a wrong schema, an empty id/title, or an unknown outcome; `selectPendingRequest` returns the newest UNANSWERED request (deterministic) so an answered ask is never re-surfaced. Self-test 5/5 gated by `handoff-request` in `verify-local-gates` (+ an inline round-trip: two requests -> the newest-pending selector -> the human's op-done -> the next pending). The extension watches `handoff/requests/` + surfaces the ask as a notification with "Mark step done" (optional note) / "Skip", both palette commands (`markStepDone`/`skipStep`), writing `handoff/done/<id>.json`; `reviewer-workstation/request-step.sh` drops the request + polls the answer ONCE. Live evidence: the agent asked the human in the reviewer VM and resumed on the op-done beacon. |
| T-057 | LBA-REQ-057 | Deterministic (offline) + live evidence | Assert the reviewer VISUAL VERDICT (ADR-0037): `buildReviewerVerdict`/`validateReviewerVerdict` derive the candidate-bound `reviewer-verdict@1` (fail-closed); `signReviewerVerdict` maps it to an `acg-human-signoff-v1` bound to the verdict digest (pass=approve); `verifyReviewerVerdict` FAILS CLOSED on a wrong schema, a tampered verdict, an un-enrolled reviewer, a mismatched key, or a bad signature; `gateVisualReview` publishes only on a pass + verified enrolled approvals; `release-with-review.mjs` composes it with the ADR-0018 machine gate; `verify-visual-review.mjs` gates a release's visualReview block. Self-test 6/6 gated by `handoff-verdict`. The extension `Render Reviewer Verdict` command + helpers (`verdictsDir`/`readReviewTarget`/`buildSignedVerdict`) are unit-covered end-to-end (the extension Ed25519-signs a verdict IN the VM that verifies against the enrolled key). Live evidence: the reviewer signs a pass verdict for ext 0.5.0 in the reviewer VM that verifies against the committed allowlist. |
| T-058 | LBA-REQ-058 | Deterministic (offline) + live evidence | Assert the reviewer verdict BUS ANNOUNCEMENT (ADR-0038): `buildVerdictBusPost` derives the semantic `lbabus` post from a signed verdict record -- pass->RESOLVED / changes->REFINE / fail->BLOCKED, `task` = `<component>-release-<version>`, `ref` = the candidate commit, `priority`; a malformed record FAILS SAFE to BLOCKED. Self-test 7/7 gated by `handoff-verdict`. The extension `busPostArgs` + `postVerdictToBus` (best-effort) + the `reviewer-workstation/post-verdict.mjs` poster are unit-covered (the FULL signed verdict JSON is the `--message-file` body). Live evidence: the ext 0.5.0 PASS verdict posted to the `lbabus` bus as a RESOLVED message on the `extension-release-0.5.0` task. |
| T-059 | LBA-REQ-059 | Deterministic (offline) + live evidence | Assert the host<->VM-agent CLOSED LOOP over lbabus net TCP (ADR-0039): `await-agent-reply.mjs`'s `parseLine` correlates the VM agent's reply by `--type` + `--task` and FAILS CLOSED on a mismatch/timeout (pure self-test 7/7); the committed `closed-loop-readback-receipt.json` records the loopback proof (a matching task closes the loop, a wrong task fails closed, the RESOLVED verdict type rides `net`) + the 3 live drives from the reviewer VM (senderId WIN); and `tools/collab-cli/Net.cs` `BusWire.Types` carries RESOLVED/REFINE/BLOCKED (option A). Self-test 7/7 gated by `closed-loop-readback` in `verify-local-gates`. Live evidence: 3 drives -- closed loop (`lbabus ready on VM`), release-with-review (ext 0.5.0 launch-to-ready 2604 ms/5 samples -> PASS via `lba-benchmark-summary`), and the signed verdict announced as a `RESOLVED: PASS` message on the `extension-release-0.5.0` task -- all over TCP, no GitHub Discussion. |
| T-060 | LBA-REQ-060 | Deterministic (offline) | Assert the LIVE-ONLY net coordination read side (ADR-0040): `net listen --log` appends received frames to a per-actor JSONL receive-log and `net poll` reads + filters it by `--type`/`--task` (mirroring the Discussion `poll` over TCP), failing closed with no `--log`/`VIHS_COLLAB_NET_LOG`; the send side is the existing `net send`. Gated by `net-coordination-log` in `verify-local-gates` (the committed `net-coordination-log-receipt.json` loopback receipt: a NOTE + a RESOLVED post -> the receive-log -> `net poll` round-trips, `--type` filtering isolates each, and `net poll` with no log FAILS CLOSED) + a Net.cs source assertion (dispatch routes `poll`->`CmdPoll`, which reads `--log`). No github.com dependency. |
| T-061 | LBA-REQ-061 | Deterministic (offline) | Assert the extension's coordination-bus TRANSPORT SELECTION (ADR-0041): `busSendArgs` builds the `lbabus net send` argv (`net send [--hosts <peers>] --type <T> --task <task> --message-file <verdict>`, omitting --hosts when no peer is configured); `busConfig` reads `busTransport` (`discussion` default | `net`) + `busNetHosts` + `busNetLog`; under `net`, `postNote`->`net send`, `pollBus`->`net poll --log`, and the verdict announcement->`busSendArgs` (reusing the semantic net types). Unit-covered in `test/extension-activation.mjs` (busSendArgs + the 24-command activation with pollBus/postNote intact). Gated by `bus-transport-select` in `verify-local-gates` (a `src/extension.ts` + `package.json` source assertion of the switch + the Discussion-safe default). No user-facing change at the default. |
| T-062 | LBA-REQ-062 | Deterministic (offline) | Assert the MCP coordination tools' TRANSPORT SELECTION (ADR-0042): `busEnvFromConfig` maps the extension's `busTransport`/`busNetHosts`/`busNetLog` to the `VIHS_COLLAB_TRANSPORT`/`VIHS_COLLAB_NET_HOSTS`/`VIHS_COLLAB_NET_LOG` env (empty -> Discussion default), and `pollBusArgs`/`postNoteArgs` route `poll_coordination_bus`->`net poll --log` + `post_coordination_note`->`net send --hosts` under `net`. Unit-covered in `test/mcp-server.mjs` (busEnvFromConfig net + Discussion cases; the provider yields a definition whose env comes from config; the transport-agnostic stdio poll/post tools stay soft-isError off-PATH). Gated by `mcp-net-transport` in `verify-local-gates` (a `src/mcp` source assertion of the switch + the env-passing provider). Tool schemas + the MCP tool doc unchanged. |
| T-063 | LBA-REQ-063 | Deterministic (offline) | Assert post-verdict.mjs's TRANSPORT SELECTION (ADR-0043): with a signed verdict record, `node post-verdict.mjs --verdict <r> --print-args` emits the Discussion `post --type RESOLVED --task extension-release-0.5.0 --priority ... --message-file` argv by default, and under `VIHS_COLLAB_TRANSPORT=net` + `VIHS_COLLAB_NET_HOSTS=10.0.2.2` emits `net send --hosts 10.0.2.2 --type RESOLVED --task extension-release-0.5.0 --message-file` (no --priority). Gated by `post-verdict-net-transport` in `verify-local-gates` (builds a signed verdict, runs --print-args under both transports, asserts the argv). The release CI (which calls --print-args) is unchanged at the Discussion default. |
| T-067 | LBA-REQ-067 | Deterministic (offline) | Assert the `lbabus` CLI carries NO GitHub-Discussion transport (ADR-0047): `tools/collab-cli/Program.cs` no longer dispatches `init`/`post`/`poll`/`wait`/`delta` and has no `CmdPost`/`CmdPoll`/`CmdWait`/`CmdInit`/`CmdDelta`, while `"net" => NetCommands.Run` + `defect`/`selfcheck` remain; `GitHubGraphQL.cs` has no Discussion GraphQL surface (`FindDiscussion`/`CreateDiscussion`/`EnsureDiscussion`/`addDiscussionComment`/`DiscussionRef`) but keeps `ListReleaseTags` + `AddIssueComment`; `Config.cs` drops `Category`/`Title`/`AddressesMe`. Gated by `cli-no-discussion-transport` in `verify-local-gates`; the collab-cli `dotnet build` + a CLI smoke test (removed commands exit 1, `net` intact) cover the runtime. |
| T-068 | LBA-REQ-068 | Deterministic (offline) + live evidence | Assert the NET-ONLY LIVE VM-AGENT DRIVE (ADR-0049): the committed `net-only-live-drive-receipt.json` (schema `net-only-live-drive-receipt@1`) seals >=1 drive from the reviewer VM (senderId `WIN`, matched) over `lbabus net` + the released-CLI net-only proof (`collab-cli-v0.15.0` rejects `init`/`post`/`poll`/`wait`/`delta`, observed `unknown command` on the VM); the verifier `net-only-live-drive.mjs` re-derives the digest + verdict deterministically and FAILS CLOSED on a non-`WIN`/unmatched drive, an incomplete net-only proof, a forged verdict, or a tampered digest (selftest 7/7). Gated by `net-only-live-drive` in `verify-local-gates`. Live evidence: 3 drives from the `actor` VM over TCP -- net-only install, benchmark re-drive (launch-to-ready 2604.2 ms/5 samples -> PASS), and the WIN sign-off for collab-cli 0.15.0 (version=0.15.0, post -> unknown command, PASS) -- no GitHub Discussion. |
| T-069 | LBA-REQ-069 | Deterministic (offline) | Assert the RELEASE-WITH-REVIEW DRIVE BINDING (ADR-0050): the committed `release-with-review-drive-receipt.json` (schema `release-with-review-drive-receipt@1`) binds a matched `WIN` staging drive over `lbabus net` (LBA-REQ-068) to a signed reviewer verdict (LBA-REQ-057) whose `target` is the SAME candidate (component/version/commit/vsixSha256) and to a `net` announce (LBA-REQ-058) correctly derived from the signed verdict; the verifier `release-with-review-drive.mjs` REUSES `verifyReviewerVerdict`/`gateVisualReview`/`buildVerdictBusPost` and FAILS CLOSED on a candidate mismatch, an unverified sign-off, a non-publishing gate, a mis-derived announce, or a tampered digest (selftest 7/7). Gated by `release-with-review-drive` in `verify-local-gates`. Committed round: ext 0.5.0 staged over net (task `rev-1785753563`), signed PASS by `reviewer@vi-tech.nl`, announced `RESOLVED` on `extension-release-0.5.0`. |
| T-070 | LBA-REQ-070 | Deterministic (offline) | Assert the COMPOSITE RELEASE DECISION (ADR-0051, capstone): the committed `composite-release-decision-receipt.json` (schema `composite-release-decision-receipt@1`) records a passing machine gate (`gateReleasePublish`: quorum PASS + an enrolled sign-off over the quorum digest) AND a passing visual gate (`gateVisualReview`: an enrolled signed PASS), and the verifier `composite-release-decision.mjs` REUSES `gateReleaseWithReview` + FAILS CLOSED unless both gates publish AND the machine quorum consensus (version + sourceCommit), the visual verdict target (component/version/commit/vsixSha256), and a matched `WIN` net staging drive all name the SAME candidate (selftest 7/7: machine-consensus unbound, visual-target unbound, failing quorum, forged visual sign-off, tampered digest). Gated by `composite-release-decision` in `verify-local-gates`. Committed round: ext 0.5.0 -- a passing 2-witness quorum + an enrolled sign-off + an enrolled signed visual PASS, all bound to the candidate staged over net (task `rev-1785753563`, senderId `WIN`), one reviewer (`reviewer@vi-tech.nl`) signing both gates. |
| T-071 | LBA-REQ-071 | Deterministic (offline) | Assert the COMPOSITE RELEASE ENFORCEMENT (ADR-0052): `tools/collab-cli/verify-composite-release.mjs --component extension <version>` REUSES the gated composite `validateReceipt` and clears the committed ext 0.5.0 candidate (exit 0) but FAILS CLOSED (exit 1) for a version with no proven composite decision; and `.github/workflows/extension-release.yml`'s `agreement` job runs the CLI (after `verify-visual-review`) with the `release` job `needs: [build, agreement]`, so the composite decision gates the publish. Gated by `composite-release-enforced` in `verify-local-gates` (the CLI clears 0.5.0 + fails closed for 0.9.9-none; the workflow wires the CLI + the release-needs-agreement dependency). Enforcement runs only on `ext-v*` tags / `workflow_dispatch`. |
| T-072 | LBA-REQ-072 | Deterministic (offline) + real committed data | Assert CROSS-PLANE LAUNCH-BENCHMARK PARITY (ADR-0053): the committed `cross-plane-launch-parity-receipt.json` (schema `cross-plane-launch-parity-receipt@1`) proves the Linux + Windows launch-to-ready trends measure the SAME benchmark via `launchIdentity` = sha256 over `{metric, workload, n}` (one LINUX + one WIN, identities match), while recording the plane means + signed delta + faster plane as performance witnesses; the verifier `launchParity.mjs` FAILS CLOSED on an identity mismatch (different workload/n), a same-plane pair, an invalid trend, or a tampered digest (selftest 7/7). Gated by `cross-plane-launch-parity` in `verify-local-gates`, which re-derives the receipt from the committed launch-trend fixtures (`experiments/launch-parity/fixtures/{linux,win}-launch-trend.json`) and asserts it reflects their means -- LINUX 2604.2 ms vs WIN 2410.6 ms (WIN faster by 193.6 ms). |
| T-073 | LBA-REQ-073 | Deterministic (offline) + real committed data | Assert MESH-RUN CROSS-PLANE FULFILLMENT (ADR-0054, roadmap Phase 3): the committed `mesh-run-fulfillment-receipt.json` (schema `mesh-run-fulfillment-receipt@1`) proves a dispatched `labview-ide-launch` run is FULFILLED iff >= `minActors` DISTINCT enrolled actors covered the requested `[LINUX, WIN]` planes, each returning a valid plane-tagged `workload-trend@1` receipt with an AGREEING benchmark identity (the LBA-REQ-072 `launchIdentity`); the verifier `meshFulfillment.mjs` FAILS CLOSED on too few actors, a duplicate actor, an uncovered plane, an invalid receipt, an identity disagreement, or a tampered digest (selftest 7/7). Gated by `mesh-run-cross-plane-fulfillment` in `verify-local-gates`. Committed run: 2 golden-VM actors -- `golden-linux` (LINUX, 2604.2 ms) + `golden-win` (WIN, 2410.6 ms) -- each embedding its real launch-trend receipt. |
| T-074 | LBA-REQ-074 | Deterministic (offline) | Assert the GITHUB-NATIVE MESH-RUN DISPATCH transport (ADR-0055): `meshDispatch.mjs` validates a committed `mesh-run-dispatch@1` request (benchmarkId + `{metric,workload,n}` + `minActors`>=1 + a non-empty valid `requestedPlanes` set + `dispatchId`, carrying the LBA-REQ-072 `launchIdentity`) and FAILS CLOSED on a missing benchmarkId, `minActors`<1, an empty/invalid planes set, or a tampered digest (selftest 7/7); the committed request BINDS to the LBA-REQ-073 fulfillment (same `benchmarkId` + identity + `minActors` + planes); and `.github/workflows/mesh-run.yml` triggers on `repository_dispatch[mesh-run]` and runs both `meshDispatch.mjs` + `meshFulfillment.mjs`. Gated by `mesh-run-dispatch-wired` in `verify-local-gates`. |
| T-075 | LBA-REQ-075 | Deterministic (offline) | Assert the MESH COVERAGE OBSERVATORY (ADR-0056): `meshObservatory.mjs` folds the committed mesh-run receipts -- dispatch (`mesh-run-dispatch@1`, LBA-REQ-074) + fulfillment (`mesh-run-fulfillment-receipt@1`, LBA-REQ-073) + cross-plane parity (`cross-plane-launch-parity-receipt@1`, LBA-REQ-072) -- into a `mesh-coverage-observatory@1` coverage matrix + consistency ledger (benchmarks x planes x actors; each run's `consistent` flag true iff dispatched + fulfilled + all artifacts name the SAME identity), and FAILS CLOSED on an identity disagreement, an un-fulfilled run counted coherent, a miscounted coverage statistic, a stale re-fold, or a tampered digest (selftest 7/7); the committed observatory re-derives byte-for-byte from the committed source receipts (currency) and its folded row is grounded in the real LBA-REQ-073 fulfillment (same identity + actor count + covered planes). Gated by `mesh-coverage-observatory` in `verify-local-gates`. |
| T-076 | LBA-REQ-076 | Deterministic (offline) | Assert the LIVE FAN-OUT contract (ADR-0057): `meshFanout.mjs` DERIVES an `actor-tasking@1` set from the committed dispatch (one identity-bound task per requested plane, carrying the LBA-REQ-072 `launchIdentity`) and validates a `receipt-collection@1` mapping each returned plane-tagged receipt back to its task, and FAILS CLOSED on an unbound task, a collected receipt with no matching task, a plane/identity mismatch, a duplicate actor, or a tampered digest (selftest 7/7); the committed tasking re-derives from the dispatch (currency), the collection RECONSTRUCTS the committed LBA-REQ-073 fulfillment (its actor set IS the fulfillment actor set), the fan-out is identity-bound end-to-end, and `.github/workflows/mesh-run.yml` runs the fan-out step. Gated by `mesh-live-fanout-wired` in `verify-local-gates`. |
| T-077 | LBA-REQ-077 | Deterministic (offline) | Assert the opt-in VERIFIED TIER (ADR-0058): `meshVerifiedTier.mjs` REUSES the ADR-0016 acg-provenance engine to attest each returned receipt with the actor's enrolled Ed25519 key, and a `verified-receipt-collection@1` admits a receipt only when it carries a valid `acg-witness-attestation-v1` from its declared, enrolled actor (verified against `mesh-actor-keys.json`), FAILING CLOSED on an unsigned/forged receipt, an un-enrolled actor, a key that does not match the enrolled one, an attestation not by the declared actor, or a tampered digest (selftest 7/7); the committed verified collection re-verifies against the committed collection + enrolled keys, every collected receipt is attested by its declared enrolled actor, and `.github/workflows/mesh-run.yml` runs the verified-tier step. Gated by `mesh-verified-tier-attested` in `verify-local-gates`. |
| T-078 | LBA-REQ-078 | Deterministic (offline) | Assert TRANSPARENCY-LOGGING the mesh-actor attestations (ADR-0059): `meshTransparency.mjs` REUSES the ADR-0022 acg-transparency engine to record each verified-tier attestation into an RFC-6962 signed Merkle tree, and a `logged-verified-collection@1` is admitted only when its signed tree head verifies against the enrolled log key (`mesh-log-key.json`) and EVERY attestation carries a valid inclusion proof against that signed root, FAILING CLOSED on an unsigned/wrong-key tree head, a missing or non-reconstructing inclusion proof, a tree-size mismatch, a verified-collection binding mismatch, or a tampered digest (selftest 7/7); the committed logged collection re-verifies, the tree logs every attestation, and `.github/workflows/mesh-run.yml` runs the transparency step. Gated by `mesh-attestations-transparency-logged` in `verify-local-gates`. |
| T-079 | LBA-REQ-079 | Deterministic (offline) | Assert the APPEND-ONLY consistency proof (ADR-0060): `meshLogHistory.mjs` REUSES the ADR-0022 acg-transparency engine to bind an earlier + the current signed tree head + an RFC-6962 consistency proof in a `logged-collection-history@1`, admitted only when both tree heads verify against the enrolled log key (`mesh-log-history-key.json`), the log STRICTLY GREW, the consistency proof proves the later tree contains the earlier unchanged (`verifyConsistency`), and the current tree head matches the committed LBA-REQ-078 log by Merkle root + size, FAILING CLOSED on an unsigned/wrong-key tree head, a non-growing/shrinking log, a tampered consistency proof, a current head that does not match the 078 log, or a tampered digest (selftest 7/7); `.github/workflows/mesh-run.yml` runs the append-only step. Gated by `mesh-log-append-only` in `verify-local-gates`. |
| T-080 | LBA-REQ-080 | Deterministic (offline) | Assert the composite MESH-RUN-ATTESTED decision (ADR-0061): `meshAttested.mjs` `decideAttested` REUSES every sub-verifier (`decideFulfillment` 073 + `validateReceipt` parity 072 + `validateVerifiedCollection` 077 + `validateLoggedCollection` 078 + `validateHistory` 079) and decides a run `attested` only when all five gates pass AND all layers name the SAME run identity; a `mesh-run-attested@1` receipt re-derives from the committed source receipts (currency), FAILING CLOSED when ANY one sub-proof is broken, on an identity mismatch, a stale gate set, a verdict contradiction, or a tampered digest (selftest 7/7, one break per sub-proof); `.github/workflows/mesh-run.yml` runs the capstone step. Gated by `mesh-run-attested` in `verify-local-gates`. |
| T-081 | LBA-REQ-081 | Deterministic (offline) | Assert cross-plane VI Analyzer PERFORMANCE PARITY (ADR-0062): `viAnalyzerParity.mjs` REUSES the LBA-REQ-072 engine (`launchIdentity`/`decideParity`/`planeSummary`/`performanceWitness`) via a `trendFromEvidence` adapter over the two committed `vi-analyzer-trend-live-evidence@1` captures (LINUX + WIN), and proves a run parity-proven only when the planes are cross-plane, share the benchmark identity `{viAnalyzerMs, vi-analyzer-labviewcli-example, n}`, AND share the deterministic resultHash (the LBA-REQ-043 link), FAILING CLOSED on an identity mismatch, a non-cross-plane pair, a differing resultHash, an invalid trend, or a tampered digest (selftest 7/7); the committed receipt re-derives byte-stably from the two evidence captures. Gated by `cross-plane-vi-analyzer-parity` in `verify-local-gates`. |
| T-082 | LBA-REQ-082 | Deterministic (offline) | Assert the BENCHMARK-SUITE PARITY OBSERVATORY (ADR-0063): `suiteParityObservatory.mjs` folds the committed cross-plane parity receipts -- launch (`cross-plane-launch-parity-receipt@1`, LBA-REQ-072) + VI Analyzer (`cross-plane-vi-analyzer-parity-receipt@1`, LBA-REQ-081) -- into a `benchmark-suite-parity-observatory@1` coverage matrix (family + identity + parity flags + LINUX/WIN performance) with `observatoryOk` iff every family is parity-proven, FAILING CLOSED on a row claiming parity without cross-plane + identity match, a miscounted coverage statistic, a verdict contradiction, or a tampered digest (selftest 7/7); the committed observatory re-folds byte-stably from the two parity receipts and each row is grounded in a real receipt identity. Gated by `benchmark-suite-parity-observatory` in `verify-local-gates`. |
| T-083 | LBA-REQ-083 | Deterministic (offline) | Assert the mesh carries a SECOND benchmark family (ADR-0064): `viAnalyzerMeshRun.mjs` REUSES `meshFulfillment` (LBA-REQ-073) + `trendFromEvidence` (LBA-REQ-081) so two golden actors return their VI Analyzer `workload-trend@1` from the committed `vi-analyzer-trend-live-evidence@1` captures and the 073 engine fulfills the run; a `mesh-benchmark-family-run@1` is `carried` only when the fulfillment is proven, the identity is the VI Analyzer identity, and it is DISTINCT from launch, FAILING CLOSED when the fulfillment is not proven, the actor receipts do not descend from the real evidence, the run is not VI Analyzer, it is not distinct from launch, or the digest is tampered (selftest 7/7); the committed run re-derives byte-stably from the two captures. Gated by `mesh-benchmark-family-vi-analyzer` in `verify-local-gates`. |
| T-084 | LBA-REQ-084 | Deterministic (offline) | Assert the STRESS-DISCOUNTED cross-plane comparison (ADR-0065): `stressDiscountedComparison.mjs` folds the committed mesh-stress ladder (`mesh-stress-live-ladder@1`, trusted only when monotone+separable+repeatable) + concurrent-actors capture (`mesh-concurrent-actors@1`, `allActorsRecovered`) into a `stress-discounted-comparison@1` that assigns each measurement a stress-quality weight (idle 1.0 .. saturate 0.0) and discounts at/above heavy, with discountingApplied only when the calibration is trustworthy, every actor recovered, and a clean/discounted split exists, FAILING CLOSED on an invalid calibration, an unrecovered actor, a weight/flag mismatch, a miscounted coverage statistic, or a tampered digest (selftest 7/7); the committed comparison re-derives byte-stably from the two mesh-stress receipts (idle full-confidence, saturate discounted to zero). Gated by `stress-discounted-comparison` in `verify-local-gates`. |
| T-085 | LBA-REQ-085 | Deterministic (offline) | Assert the BYTE-REPRODUCIBLE extension package (ADR-0066): `scripts/normalize-vsix.mjs` (pure Node, no deps) walks the zip (EOCD -> each central-directory record -> the local file header it points to) and patches ONLY the 2-byte DOS mod-time + mod-date to 1980-01-01, leaving names/order/compression/content/CRCs untouched, so `npm run package` (which runs it after `vsce package`) yields a `.vsix` whose sha256 depends only on the committed content, not the build time. `test/normalize-vsix.mjs` (run by `npm test`) builds two zips with identical content but different entry timestamps and proves they normalize to BYTE-IDENTICAL output, that normalization is idempotent, that the DOS timestamp is epoch-pinned, and that a non-zip buffer FAILS CLOSED. Gated by `reproducible-vsix-normalizer` in `verify-local-gates` (guards the package+test wiring + re-proves the behavior synchronously on a hand-built stored-entry zip). |
| T-086 | LBA-REQ-086 | Deterministic (offline) + dual-OS CI | Assert the CROSS-PLANE byte-reproducible extension package (ADR-0067): `scripts/normalize-vsix.mjs` pins every entry's OS-dependent zip metadata -- DOS mod-time/date (1980-01-01), external file attributes (regular file 0644), version-made-by (Unix) -- and `.gitattributes` + `tsconfig.json` (`newLine: lf`) force LF on the packaged content, so a Windows build and a Linux build of the same commit are BYTE-IDENTICAL. `.github/workflows/vsix-cross-plane-repro.yml` builds `npm run package` on `ubuntu-latest` AND `windows-latest` and FAILS CLOSED unless the two sha256 match. The offline gate `vsix-cross-plane-repro-workflow-wired` (in `verify-local-gates`) proves the workflow builds on both planes + compares the sha + fails closed + the `newLine: lf` / LF-`.gitattributes` prerequisites hold; `test/normalize-vsix.mjs` (run by `npm test`) asserts the central-directory version-made-by + external attributes are pinned. |
| T-087 | LBA-REQ-087 | Deterministic (offline) + dual-OS CI | Assert GENUINE cross-plane corroboration (ADR-0069): windows-latest is a genuine windows plane, so `experiments/acg-quorum/produce-witness.mjs` emits an acg-witness-bundle-v1 from the current plane over the deterministic anchors (version/sourceCommit/gate verdict/viewer seriesHash -- the shipped viewer's projection of the committed mprr fixture, identical on every plane); pngSha256 is an optional Linux-only anchor. `.github/workflows/acg-cross-plane-corroboration.yml` produces a witness on a multi-substrate matrix (`ubuntu-22.04` + `ubuntu-24.04` on the linux plane, `windows-2022` + `windows-2025` on the windows plane, each after `npm test`) and `corroborate-planes.mjs` FAILS CLOSED unless ALL substrates concur AND span distinct OS-planes (crossPlane) -- proving the anchor is substrate-independent. `produce-witness.selftest.mjs` (run by gate `acg-cross-plane-corroboration`) proves a linux+windows pair corroborates while a single-plane, divergent, or non-pass pair fails closed; the drift gate `acg-cross-plane-corroboration-workflow-wired` keeps the workflow wired. |
| T-088 | LBA-REQ-088 | Deterministic (offline) | Assert the DURABLE cross-plane corroboration attestation (ADR-0070): `experiments/acg-quorum/cross-plane-attestation.mjs` embeds two GENUINE CI witnesses (a real linux plane = ubuntu-latest + a real windows plane = windows-latest, run 30923501292 @ 2a0352c) + their provenance, re-derives the os-plane quorum (`compare-witnesses.mjs`), and is CROSS-PLANE CORROBORATED only when the quorum PASSES + spans both os-planes (crossPlane); a canonical digest makes it tamper-evident. `cross-plane-attestation.selftest.mjs` (run by gate `acg-cross-plane-attestation`) proves the committed attestation validates while a SINGLE-PLANE set (the 1.0.0 defect: two linux witnesses), a non-pass verdict, a tampered witness, a forged verdict, and a tampered digest all fail closed. |
| T-089 | LBA-REQ-089 | Deterministic (offline) | Assert the SIGNED cross-plane corroboration re-seal (ADR-0071): `experiments/acg-quorum/signed-cross-plane-corroboration.mjs` REUSES gateReleasePublish (ADR-0018: quorum pass + a verified enrolled sign-off over the quorum digest) and additionally requires the quorum be `crossPlane` and name the candidate; the committed `signed-cross-plane-corroboration-receipt.json` records extension 1.0.0 @ 2a0352c + the enrolled reviewer@vi-tech.nl sign-off (produced offline by `reviewer-workstation/sign-release-quorum.mjs`, private key local). `signed-cross-plane-corroboration.selftest.mjs` (run by gate `acg-signed-cross-plane-corroboration`) proves a signed crossPlane quorum validates while a single-plane, non-pass, un-enrolled, forged, unnamed, or tampered receipt all fail closed. |
| T-090 | LBA-REQ-090 | Deterministic (offline) | Assert the GENUINE cross-plane COMPOSITE re-seal (ADR-0072/0073): `reviewer-workstation/composite-release-decision-receipt.json` (collapsed to the crossPlane re-seal) is assembled via the REUSED composite verifier (`composite-release-decision.mjs`) from the crossPlane machine quorum (LBA-REQ-088) + the enrolled machine sign-off (LBA-REQ-089) + a signed WINDOWS_VM visual PASS of the byte-reproducible candidate (vsix 2ec7bd31 @ 2a0352c) + the genuine WIN net-staged frame -- all five bindings hold, the quorum is `crossPlane`, both gates signed by enrolled reviewer@vi-tech.nl. `crossplane-composite-reseal.selftest.mjs` (run by gate `acg-crossplane-composite-reseal`) proves the committed crossPlane composite validates as a proven composite decision AND its quorum is crossPlane, and `verify-composite-release` clears it while rejecting a single-plane composite (the 1.0.0 defect). |
| T-091 | LBA-REQ-091 | Deterministic (offline) | Assert RUN-BOUND mesh ingestion (ADR-0074): `meshIngest.mjs` ingests a LIVE dispatch (the workflow `client_payload`, validated via the LBA-REQ-074 `requestOk` + `identity === dispatchIdentity(benchmark)`) + a folder of `returned-receipt@1` files, REUSING the LBA-REQ-076 fan-out (`deriveTasking` + `buildCollection` + `validateTasking` + `validateCollection`) to produce a run-bound `actor-tasking@1` + `receipt-collection@1` bound to the `dispatchId`, and FAILS CLOSED on an uncovered requested plane, a declared/receipt plane mismatch, a returned receipt whose identity != the dispatched benchmark, a receipt bound to an unknown task, a duplicate actor, a malformed dispatch, or a malformed returned receipt (selftest 8/8). Gated by `mesh-run-ingest` in `verify-local-gates`. |
| T-092 | LBA-REQ-092 | Deterministic (offline) | Assert RUN-BOUND CROSS-PLANE CORROBORATE + COMPARE (ADR-0075): `meshCorroborate.mjs` (`corroborateRun`) consumes the LBA-REQ-091 run-bound `receipt-collection@1` and corroborates the collected plane receipts cross-plane -- >= 2 distinct OS-planes, every plane's `workload-trend@1` PASSES, each `receipt.plane` matches its collected plane, and each re-derives `dispatchIdentity{metric,workload,n}` = the collection identity -- then REUSES benchmark-store `compareRuns` (LBA-REQ-010) for the LINUX-baseline-vs-WIN-candidate `cross-plane-compare@v1` delta, emitting a run-bound `mesh-cross-plane-report@1`, and FAILS CLOSED on a single-plane collection, a non-PASS plane, an identity mismatch (a plane ran a different benchmark), a malformed collection, a non-trend receipt, or a receipt/collected plane mismatch (selftest 8/8). The committed two-plane fan-out collection corroborates (LINUX+WIN). Gated by `mesh-cross-plane-corroborate` in `verify-local-gates`. | Deterministic (offline) | Assert the coordination product surface is NET-ONLY (ADR-0046): `package.json` no longer contributes `labviewBenchmarkActor.busTransport` (only `busNetHosts`/`busNetLog`); `src/extension.ts` has no `busPostArgs` / `busTransport` selection and routes Poll Bus -> `net poll` + Post Note/verdict -> `net send`; `src/mcp/*` `busEnvFromConfig` maps only `VIHS_COLLAB_NET_HOSTS`/`VIHS_COLLAB_NET_LOG` and `pollBusArgs`/`postNoteArgs` are net-only (no `VIHS_COLLAB_TRANSPORT`); `post-verdict.mjs --print-args` emits `net send ...` (with a peer -> `--hosts`, without -> `--skip-if-no-peer`, never `post `). Covered by `npm test` (extension activation + `busSendArgs` net-only; `busPostArgs` removed; MCP `busEnvFromConfig` net-only) + gated by `bus-transport-select` + `mcp-net-transport` + `post-verdict-net-transport` (net-only) + `net-default-graceful` in `verify-local-gates`. |
| T-093 | LBA-REQ-093 | Deterministic (offline) | Assert the NODE-VERSION-PINNED reproducible package (issue #408): a repo-root `.nvmrc` pins the exact release Node version and `.github/workflows/extension-release.yml`, `.github/workflows/vsix-cross-plane-repro.yml`, and `.github/workflows/acg-cross-plane-corroboration.yml` each source Node via `node-version-file: .nvmrc` with no floating `node-version:` literal, so the reviewed (local) build and the CI publish build resolve the SAME Node and the `.vsix` cannot drift across a Node minor. `lba release-preflight` asserts the local Node equals `.nvmrc` (an equal Node clears, a later 24.x minor fails). Gated by `release-path-node-pinned` in `verify-local-gates` (proves `.nvmrc` pins an exact version + every release-path workflow sources it) + the `scripts/lba.mjs` selftest (the exact-version preflight). |
| T-065 | LBA-REQ-065 | Deterministic (offline) | Assert the coordination default is `net` with a graceful no-op when unconfigured (ADR-0045): `package.json` `labviewBenchmarkActor.busTransport` defaults to `net` (Discussion is the legacy opt-out), the extension/MCP provider+stdio/post-verdict default to net, and the CLI degrades gracefully -- `Net.cs` exits 0 with a hint for `net poll` with no receive-log (softened from fail-closed) and for `net send --skip-if-no-peer` with no peer (no dead loopback); the extension routes the send side via `--skip-if-no-peer` when no host is set. Covered by `npm test` (extension activation + `busSendArgs` no-peer omits `--hosts`; MCP busEnvFromConfig net default) + the `net-coordination-log` receipt (`pollWithoutLogGraceful`). Gated by `net-default-graceful` (Net.cs graceful branches + net default in package.json/extension) + `bus-transport-select` (default === 'net') in `verify-local-gates`. |
| T-064 | LBA-REQ-064 | Deterministic (offline) | Assert the release publish workflow carries NO GitHub-Discussion verdict announce (ADR-0044): `.github/workflows/extension-release.yml` has no `dotnet run ... LbaBus.csproj` and no `Announce the reviewer verdict on the coordination bus` step, documents the COMMITTED signed verdict as the durable record (LBA-REQ-064), and RETAINS the `Stage the signed reviewer verdict for keyless counter-sign` step. Gated by `release-no-discussion-announce` in `verify-local-gates`. The verdict is still announced off-CI by the extension + post-verdict.mjs over net (LBA-REQ-061/063); this removes the last GitHub-Discussion dependency from the publish pipeline. |
| T-028 | LBA-REQ-028 | Deterministic (offline) + live loopback evidence | Assert a witness verdict beacons as a valid `bus-msg@1` NOTE (task `acg-verdict`, carrying the small verdict + bundle digest, comms-only), survives the real 4-byte length-prefixed wire round-trip (`encodeFrame`/`createFrameDecoder`), and FAILS CLOSED on a wrong schema/task or a missing witness/digest/verdict; the `MeshLedger` dedups by witness (highest seq wins, stale rejected) with a tamper-evident `ledgerHash`; and `quorumFromLedger` resolves each beaconed witness to its bundle by digest (fail-closed on missing/mismatch) then runs the quorum. Self-test 8/8 gated by `acg-mesh-verdict-beacon`. Live loopback evidence: the real {codespace, host} verdicts beaconed over `bus-msg@1` 127.0.0.1 TCP -> ledger -> quorum pass (0.917), committed as `mesh-loopback-receipt.json` and re-derived tamper-evidently by `acg-mesh-loopback-evidence`. |
| T-029 | LBA-REQ-029 | Deterministic (offline) | Assert the ACG MCP surface (`grid-tools.mjs`) exposes the grid operations over the JSON-RPC 2.0 MCP contract: `initialize` returns the protocol + serverInfo, `tools/list` publishes the ADR-0020 tools (`spin_up_witness`/`run_quorum`/`get_confidence`/`verify_attestation`/`teardown`/`verify_inclusion`/`verify_before_install`), and `tools/call` composes the engines (run_quorum corroborates; get_confidence returns the graded confidence; verify_inclusion + verify_before_install verify transparency-log inclusion per ADR-0022 and fail closed on a mismatch/sub-quorum; spin_up_witness/teardown return provisioning plans), failing closed on an unknown tool (-32602) with tool-execution failures riding `isError` in the result envelope. Also spawn `server.mjs` and assert a real stdin/stdout JSON-RPC round-trip. Self-test 13/13 gated by `acg-mcp-grid-surface` in `verify-local-gates`. |
| T-027 | LBA-REQ-027 | Deterministic (offline) | Assert the human sign-off gate (`sign-off.mjs`) records an Ed25519 sign-off (from either reviewer station, WINDOWS_VM or LINUX_CODESPACE) over the exact machine-quorum verdict, and that `gateReleasePublish` allows publish ONLY when the machine quorum passes AND >= minReviewers distinct enrolled reviewers have a verified `approve` sign-off -- BLOCKING on a missing sign-off, an un-enrolled reviewer, a `reject` decision, a failing quorum (the sign-off does not substitute for it), a replayed sign-off over another verdict, a tampered signature, or an unknown station; multi-reviewer-ready. Self-test 10/10 gated by `acg-reviewer-sign-off`. Live evidence: the real corroborated {codespace, host} release is BLOCKED pending human sign-off (publish:false, quorumPass:true), re-derived by `acg-reviewer-release-decision`. |
| T-023 | LBA-REQ-023 | Deterministic (offline) | Assert the Actor Corroboration Grid end-to-end gate (`grid.mjs` `runGrid`) composes every sub-engine into one release decision: a release is machine-corroborated ONLY when independence (distinct enrolled environments) AND the quorum (majority over the anchors) AND the attestation (verify-before-consume) AND the mesh (beacon->ledger->quorum) all pass, and RELEASED only when a recorded human sign-off also accompanies the verdict -- failing closed on an N-of-a-kind set, a sub-majority quorum, a tampered attestation, or a missing sign-off. Self-test 6/6 gated by `acg-grid-e2e`. Live evidence: the real {codespace, host} grid corroborates through every machine stage (machineCorroborated:true) and is held only at the human gate (released:false), re-derived by `acg-grid-run-live`. |

## Browser / UI validation

- The cursor and picture-panel behavior is verified in a real browser
  (headless is acceptable) over a synthetic run result, mirroring the parent
  repo's preview-viewer harness practice — assert cursor tracking, keyboard
  paging, synchronized picture updates, and the no-frame state, not just
  Node-level logic. `[Assumption]` browser harness stays out of hosted CI (it
  ships as a maintainer harness), consistent with `vi-history-suite`.

## Test coverage (PR Coverage Gate)

Test coverage is measured with **c8** (V8 coverage) and enforced by the **PR Coverage Gate**
(`.github/workflows/coverage.yml`, status context `PR Coverage Gate / coverage`): `npm run test:coverage`
runs the suite under c8, emits a **Cobertura** `coverage/cobertura-coverage.xml` (the retained coverage
artifact), and FAILS below the line-coverage **threshold** (fail-under). The thresholds are parametrized in
`coverage-thresholds.json` (the single knob) and RATCHET upward gradually -- `npm run coverage:bump` raises
each floor toward the measured coverage by at most `step`, capped at `target`, and never lowers a floor.
Adoption floors: lines/statements 70%, functions 58%, branches 45% (measured 73/73/62/49%). Gated locally by
`coverage-artifact-meets-floor` in `verify-local-gates`.

## Local CI/CD verification (local gate)

Local CI/CD **is** testing for this package: the retained experiment receipts
and the RTM `Proven` evidence are re-validated by a real, re-runnable pass/fail
gate rather than trusted as static files.

- **Gate:** `node experiments/verify-local-gates.mjs` (dependency-free ESM). It
  asserts the bus-prototype receipt is green (12/12), the OCR-primitive engine
  is available with byte-exact readback, the shared retained inputs
  (`ground-truth-ledger.json`, `surface-metadata.json`) are present, every
  RTM `Proven` row cites an existing evidence path, and the CPU/RAM/disk
  resource-usage correlation receipt is green (LBA-REQ-011). Exit code is
  non-zero on any failure.
- **Cross-platform by design.** The seeded workflow
  `.github/workflows/lba-local-gates.yml` runs the gate on **both** a
  `linux-native` and a `windows-native` runner. That parity is the near-term
  horizon — linux-native mirroring the same mprr **ring-buffer** read/replay
  capability windows-native has (best effort). The ring-buffer read/replay path
  is already cross-platform (the mprr `ReviewCaptureTransportReader` targets
  `net8.0` plain, build-proven on windows-native); only surface render and the
  `Windows.Media.Ocr` image-derived-timing production remain windows-bound.
- The workflow is **dormant** while the package is a subtree and activates at
  the standalone repository root (LBA-REQ-008, `docs/cm/cm-plan.md` move step 2).

## Entry / exit criteria (29119-2)

- **Entry:** the run-result schema and bus message schema are frozen for the
  slice under test.
- **Exit:** every `LBA-REQ` under the slice has a passing deterministic test;
  the local CI/CD gate (`experiments/verify-local-gates.mjs`) is green on both
  runners; the deterministic-logic suites enforce a line-coverage **threshold**
  of at least 75% (`fail-under` 75% in local CI/CD) once the actor logic is
  implemented; transport and deployment items are validated on the real targets
  and recorded as maintainer evidence.
