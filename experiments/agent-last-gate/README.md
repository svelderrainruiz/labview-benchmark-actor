# Agent last gate evidence

`scripts/agent-last-gate.mjs` writes its generated, git-ignored `receipt.json` into this directory. The maintained
runner is an active release surface; the receipt is per-run evidence and is regenerated before human review.
