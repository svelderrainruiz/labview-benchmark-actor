name: collab-cli-release

# Publishes the shared `lbabus` coordination-bus CLI as an immutable, SemVer-tagged GitHub Release.
# Both the WIN and LINUX planes pull the SAME pinned version so they cannot drift.
#
# Trigger: push a tag `collab-cli-vX.Y.Z` whose X.Y.Z matches <Version> in LbaBus.csproj.

on:
  push:
    tags:
      - 'collab-cli-v*.*.*'

permissions:
  contents: write

jobs:
  harness:
    # Gate the release on the Docker-CI edge-case harness. Each target runs the runner INSIDE the image
    # build (RUN lbabus-ci ...), so a failing case fails the build and blocks the publish below.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Harness — ripgrep present (offline cases pass, mock cases skip)
        run: docker build -f tools/collab-cli/ci/Dockerfile --target ci-rg .

      - name: Harness — ripgrep absent (ripgrep-only fail-closed case runs)
        run: docker build -f tools/collab-cli/ci/Dockerfile --target ci-no-rg .

      - name: Harness — musl/Alpine (framework-dependent lbabus runs green on musl)
        run: docker build -f tools/collab-cli/ci/Dockerfile --target ci-musl .

      - name: Harness — musl RID contract (glibc self-contained native fails on musl)
        run: docker build -f tools/collab-cli/ci/Dockerfile --target ci-musl-native .

      - name: Harness — cross-process lease mutual-exclusion (concurrency gate)
        run: docker build -f tools/collab-cli/ci/Dockerfile --target ci-stress .

      - name: Harness — agent base-instructions embed round-trip + drift detection
        run: docker build -f tools/collab-cli/ci/Dockerfile --target ci-agents .

      - name: Harness — documentation package embed round-trip + drift detection
        run: docker build -f tools/collab-cli/ci/Dockerfile --target ci-docs .

      - name: Harness — spec-vs-impl requirements-coverage (RTM gap ledger enforced)
        run: docker build -f tools/collab-cli/ci/Dockerfile --target ci-reqs .

  # Bidirectional agreement gate (operator directive): a shared bus release publishes only after
  # BOTH the WIN and LINUX planes have committed agreed:true for this exact version in
  # release-agreement.json. Fails closed, so neither plane can unilaterally ship.
  agreement:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Derive version from tag
        id: ver
        run: |
          tag="${GITHUB_REF_NAME}"
          echo "version=${tag#collab-cli-v}" >> "$GITHUB_OUTPUT"

      - name: Verify bidirectional WIN <-> LINUX release agreement
        run: node tools/collab-cli/verify-release-agreement.mjs "${{ steps.ver.outputs.version }}"

  release:
    needs: [harness, agreement]
    runs-on: ubuntu-latest
    permissions:
      contents: write # create the release + attach assets
      id-token: write # keyless-sign the artifacts with cosign (Fulcio OIDC -> public rekor)
    defaults:
      run:
        working-directory: tools/collab-cli
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '8.0.x'

      - name: Derive version from tag
        id: ver
        run: |
          tag="${GITHUB_REF_NAME}"
          version="${tag#collab-cli-v}"
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "Building lbabus ${version} from tag ${tag}"

      - name: Pack dotnet tool
        run: dotnet pack -c Release -p:Version=${{ steps.ver.outputs.version }} -o ../../artifacts

      - name: Publish self-contained (linux-x64)
        run: >
          dotnet publish -c Release -r linux-x64 --self-contained true
          -p:PublishSingleFile=true -p:Version=${{ steps.ver.outputs.version }}
          -o ../../artifacts/linux-x64

      - name: Publish self-contained (win-x64)
        run: >
          dotnet publish -c Release -r win-x64 --self-contained true
          -p:PublishSingleFile=true -p:Version=${{ steps.ver.outputs.version }}
          -o ../../artifacts/win-x64

      - name: Stage release assets
        working-directory: .
        run: |
          set -eu
          mkdir -p staging
          cp artifacts/*.nupkg staging/
          cp "artifacts/linux-x64/lbabus" "staging/lbabus-${{ steps.ver.outputs.version }}-linux-x64"
          cp "artifacts/win-x64/lbabus.exe" "staging/lbabus-${{ steps.ver.outputs.version }}-win-x64.exe"
          ls -l staging

      - name: Extract release notes from CHANGELOG
        working-directory: .
        id: notes
        run: |
          set -eu
          version="${{ steps.ver.outputs.version }}"
          changelog="tools/collab-cli/CHANGELOG.md"
          notes_file="${RUNNER_TEMP}/release-notes.md"
          # Extract just this version's section (header through the next "## [" header).
          if [ -f "$changelog" ]; then
            awk -v ver="$version" '
              $0 ~ ("^## \\[" ver "\\]") { grab=1; print; next }
              grab && /^## \[/ { exit }
              grab { print }
            ' "$changelog" > "$notes_file"
          fi
          # Fail-safe: fall back to the generic note if the section is missing/empty
          # so a CHANGELOG gap can never fail the release.
          if [ ! -s "$notes_file" ]; then
            printf 'Shared cross-plane coordination bus CLI. Immutable SemVer release; both planes install this exact version.\n' > "$notes_file"
          fi
          echo "Release notes for ${version}:"
          cat "$notes_file"
          echo "path=${notes_file}" >> "$GITHUB_OUTPUT"

      - name: Keyless-attest the release artifacts (Fulcio + public rekor)
        # Sign every staged asset (nupkg + the linux/win lbabus binaries) keylessly (cosign, Actions OIDC) so
        # the release carries Fulcio certs + public rekor entries, attached AT CREATION (immutable-safe).
        uses: ./.github/actions/keyless-attest
        with:
          artifacts-dir: ${{ github.workspace }}/staging

      - name: Create GitHub Release
        working-directory: .
        env:
          GH_TOKEN: ${{ github.token }}
        run: >
          gh release create "${GITHUB_REF_NAME}"
          --repo "${GITHUB_REPOSITORY}"
          --title "lbabus ${{ steps.ver.outputs.version }}"
          --notes-file "${{ steps.notes.outputs.path }}"
          staging/*
