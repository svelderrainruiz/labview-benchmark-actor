node_modules/
out/
*.vsix
media/viewerCursor.mjs
media/counter-render.mjs
media/mprr-series.json
media/AGENTS.md
media/agents.manifest.json
media/benchmark-panels.mjs
media/launch-capture.mjs
media/frame-correlator.mjs
media/captureStatus.mjs
media/handoffRequest.mjs
media/reviewerVerdict.mjs
media/labview-launch-record.json
media/labview-launch-trend.json
media/labview-launch-trend-win.json
media/cross-plane-trend-receipt.json
media/labview-launch-resource-correlation.json
media/resource-cross-plane-receipt.json
media/meshCalibrationView.mjs
media/mesh-live-ladder-receipt.json
media/meshBoardView.mjs
media/mesh-concurrent-actors-receipt.json
# deterministic-screenshot PNG artifacts (evidence; the small JSON receipt is committed, the PNG is not)
playwright/.artifacts/
# host-extracted NI LabVIEW offline feeds (licensed/community artifacts staged for the docker-windows mirror)
cleanroom/docker-windows/.lv-feed/
cleanroom/docker-windows/.lv-cli-feed/
# Vagrant runtime state for the reviewer-workstation VM
reviewer-workstation/.vagrant/
# Host-side GitHub token handed off to the reviewer VM for INTERNAL-repo release downloads (NEVER commit)
reviewer-workstation/.gh-token
**/.gh-token

# Ubuntu clean-room per-actor credential store (dev-only passwords; user-owned property; NEVER commit).
# The committed `mesh-actors.csv.example` is the schema template; the filled-in real file is ignored.
cleanroom/ubuntu-labview/mesh-actors.csv
cleanroom/ubuntu-labview/.actor-pw
# Ubuntu clean-room local build artifacts + Vagrant mesh runtime state
cleanroom/ubuntu-labview/.build/
cleanroom/ubuntu-labview/mesh/.vagrant/
.devcontainer/cleanroom-witness/.source-commit

# Agent-last-gate per-run verdict receipt (regenerated each run)
experiments/agent-last-gate/receipt.json

# Local Windows-container TightVNC experiment evidence (logs, screenshots, records, and receipts)
experiments/windows-docker-container/evidence/

# Coverage: keep the retained artifact coverage/cobertura-coverage.xml; ignore raw V8 tmp + transient summary.
coverage/tmp/
coverage/coverage-summary.json
coverage/coverage-final.json
.nyc_output/

# .NET "runfile discovery" tooling cache the C# extension writes into the workspace root (codespace/cleanroom noise)
/dotnet/
.lba/
